# admin-ui/.env.development
# 这个文件用于本地开发环境的环境变量。
# 这些变量会覆盖 .env 文件中定义的同名变量。
# Vite 会在开发模式下 (npm run dev) 加载这些变量。

# API 服务的本地开发环境基础 URL
# 为了配合 Vite 代理，这里应使用相对路径，代理会将请求转发到实际的后端地址。
VITE_API_BASE_URL=/api/v1

# 其他开发环境特定的配置
# VITE_APP_TITLE=My Development App
# VITE_DEBUG_MODE=true
